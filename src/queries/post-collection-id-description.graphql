query PostCollection {
    postCollection(
        order: [sys_publishedAt_DESC]
        preview: false
    ) {
        items {
            sys {
                publishedAt
            }
            id
            title
            description
        }
    }
}

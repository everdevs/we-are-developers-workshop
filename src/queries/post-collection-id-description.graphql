query PostCollection {
    postCollection(
        order: [sys_publishedAt_DESC]
        preview: false
    ) {
        items {
            sys {
                id
                publishedAt
            }
            title
            description
        }
    }
}
